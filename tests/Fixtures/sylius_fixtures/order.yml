sylius_fixtures:
    suites:
        order:
            listeners:
                suite_loader:
                    options:
                        suites:
                            - "data_provider_test"
            fixtures:
                new_order:
                    name: custom_order
                    options:
                        amount: 1
                        channel: 'SKLEP'
                        customer: 'klient@example.com'
                        country: 'PL'
                        fulfilled: true
                        items: ['pasztetowa']
